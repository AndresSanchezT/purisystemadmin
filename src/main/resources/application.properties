# ==============================
# CONFIGURACIÓN DEL SERVIDOR
# ==============================
server.port=8080
spring.application.name=SISTEMASPURI

# ==============================
# CONFIGURACIÓN DE BASE DE DATOS (MySQL)
# ==============================
spring.datasource.url=jdbc:mysql://localhost:3306/puribd?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==============================
# JPA / HIBERNATE
# ==============================
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ==============================
# LOGGING
# ==============================
logging.level.org.springframework=INFO
logging.level.com.tuempresa.lacteos=DEBUG

# ==============================
# CONFIGURACIÓN DE JSON (Gson o Jackson)
# ==============================
# spring.mvc.converters.preferred-json-mapper=jackson

# ==============================
# CONFIGURACIÓN DE SEGURIDAD (JWT / OAUTH2 opcional)
# ==============================
# spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8080/auth/realms/lacteos

# ==============================
# CONFIGURACIÓN DE ARCHIVOS (para facturas, comprobantes, etc.)
# ==============================
# file.upload-dir=uploads/
